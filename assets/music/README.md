# Background music library

Drop royalty-free MP3/WAV/M4A files here. The slideshow pipeline picks one at random per video and mixes it under narration during Remotion export.

If this folder is empty, the pipeline also checks `music/` at the repo root.

Override the folder with `MUSIC_DIR` in `.env` (default: `assets/music`).
